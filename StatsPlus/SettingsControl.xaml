<UserControl x:Class="StatsPlus.SettingsControl"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:StatsPlus"
             xmlns:styles="clr-namespace:SimHub.Plugins.Styles;assembly=SimHub.Plugins"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <local:TimeSpanSecondsFormatter x:Key="TimeSpanSecondsFormatter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Margin="0,0,0,8"
                Padding="12,10"
                Background="#252525"
                BorderBrush="#2CB7F0"
                BorderThickness="1"
                CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="120" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Foreground="#89D9FF" FontSize="12" FontWeight="SemiBold" Text="Live status" />
                    <TextBlock Foreground="{Binding StatusSectionForeground}" FontSize="18" FontWeight="Bold" Text="{Binding LiveStatusLabel}" TextTrimming="CharacterEllipsis" />
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="16,0,16,0">
                    <TextBlock Foreground="{Binding StatusSectionForeground}" FontSize="12" FontWeight="SemiBold" Text="{Binding DataStatus}" TextTrimming="CharacterEllipsis" />
                    <TextBlock Margin="0,4,0,0" Foreground="#D8D8D8" FontSize="12" Text="{Binding CurrentContext}" TextTrimming="CharacterEllipsis" />
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Foreground="#89D9FF" FontSize="12" FontWeight="SemiBold" Text="Session laps" />
                    <TextBlock Foreground="#D8D8D8" FontSize="18" FontWeight="Bold" Text="{Binding SessionLapCount}" />
                </StackPanel>

                <StackPanel Grid.Column="3">
                    <TextBlock Foreground="#89D9FF" FontSize="12" FontWeight="SemiBold" Text="Last" />
                    <TextBlock Foreground="#D8D8D8" FontSize="18" FontWeight="Bold" Text="{Binding LastLapSeconds, Converter={StaticResource TimeSpanSecondsFormatter}}" />
                </StackPanel>

                <StackPanel Grid.Column="4">
                    <TextBlock Foreground="#89D9FF" FontSize="12" FontWeight="SemiBold" Text="Session best" />
                    <TextBlock Foreground="#D8D8D8" FontSize="18" FontWeight="Bold" Text="{Binding SessionBestLapSeconds, Converter={StaticResource TimeSpanSecondsFormatter}}" />
                </StackPanel>

                <StackPanel Grid.Column="5">
                    <TextBlock Foreground="#89D9FF" FontSize="12" FontWeight="SemiBold" Text="All-time best" />
                    <TextBlock Foreground="#D8D8D8" FontSize="18" FontWeight="Bold" Text="{Binding AllTimeBestLapSeconds, Converter={StaticResource TimeSpanSecondsFormatter}}" />
                </StackPanel>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <styles:SHTabControl ItemsSource="{Binding TopLevelTabs}"
                                 SelectedItem="{Binding SelectedTopLevelTab, Mode=TwoWay}">
                <styles:SHTabControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Header}" />
                    </DataTemplate>
                </styles:SHTabControl.ItemTemplate>

                <styles:SHTabControl.Resources>
                    <DataTemplate DataType="{x:Type local:GameHistoryTab}">
                        <StackPanel>
                            <styles:SHSection Title="Stored History">
                                <StackPanel Margin="12">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                        <Button Width="140"
                                                Margin="0,0,10,0"
                                                HorizontalAlignment="Left"
                                                Click="RefreshHistoryButton_Click"
                                                Content="Refresh history" />
                                        <Button Width="150"
                                                Margin="0,0,10,0"
                                                HorizontalAlignment="Left"
                                                IsEnabled="{Binding DataContext.HasSelectedGameHistoryTab, ElementName=Root}"
                                                Click="ClearSelectedGameButton_Click"
                                                Content="Clear selected game" />
                                        <Button Width="120"
                                                Margin="14,0,0,0"
                                                HorizontalAlignment="Left"
                                                Click="ClearAllDataButton_Click"
                                                Content="Clear all data" />
                                    </StackPanel>

                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <ComboBox Grid.Column="0"
                                                  Width="125"
                                                  ItemsSource="{Binding SearchFieldOptions}"
                                                  DisplayMemberPath="Label"
                                                  SelectedValuePath="Field"
                                                  SelectedValue="{Binding SelectedSearchField, Mode=TwoWay}" />
                                        <TextBox Grid.Column="1"
                                                 MinWidth="220"
                                                 Margin="8,0"
                                                 ToolTip="Search car, circuit, or layout"
                                                 Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                        <Button Grid.Column="2"
                                                Width="70"
                                                Click="ClearHistorySearchButton_Click"
                                                Content="Clear" />
                                    </Grid>

                                    <TextBlock Margin="0,0,0,8"
                                               Foreground="#D8D8D8"
                                               FontStyle="Italic"
                                               Text="No matching history."
                                               Visibility="{Binding HasNoMatchingHistory, Converter={StaticResource BooleanToVisibilityConverter}}" />

                                    <DataGrid ItemsSource="{Binding FilteredTracks}"
                                              AutoGenerateColumns="False"
                                              CanUserAddRows="False"
                                              IsReadOnly="True"
                                              Height="300"
                                              SelectedItem="{Binding DataContext.SelectedTrackSummary, ElementName=Root, Mode=TwoWay}">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Car" Binding="{Binding CarModelDisplay}" Width="180" />
                                            <DataGridTextColumn Header="Circuit Name" Binding="{Binding CircuitNameDisplay}" Width="180" />
                                            <DataGridTextColumn Header="Circuit Layout" Binding="{Binding CircuitLayoutDisplay}" Width="180" />
                                            <DataGridTextColumn Header="Laps" Binding="{Binding LapCount}" Width="60" />
                                            <DataGridTextColumn Header="Best" Binding="{Binding BestLapSeconds, Converter={StaticResource TimeSpanSecondsFormatter}}" Width="90">
                                                <DataGridTextColumn.ElementStyle>
                                                    <Style TargetType="{x:Type TextBlock}">
                                                        <Setter Property="Foreground" Value="LimeGreen" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsBestLapValid}" Value="False">
                                                                <Setter Property="Foreground" Value="Red" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </DataGridTextColumn.ElementStyle>
                                            </DataGridTextColumn>
                                            <DataGridTextColumn Header="Last UTC" Binding="{Binding LastRecordedUtc, StringFormat={}{0:u}}" Width="150" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </StackPanel>
                            </styles:SHSection>

                            <styles:SHSection Title="Recorded Laps">
                                <StackPanel Margin="12">
                                    <TextBlock Margin="0,0,0,8"
                                               FontWeight="Bold"
                                               Text="{Binding DataContext.SelectedTrackCaption, ElementName=Root}" />
                                    <Button Width="170"
                                            Margin="0,0,0,10"
                                            HorizontalAlignment="Left"
                                            IsEnabled="{Binding DataContext.HasSelectedLap, ElementName=Root}"
                                            Click="ToggleLapValidityButton_Click"
                                            Content="Toggle selected lap valid" />
                                    <DataGrid ItemsSource="{Binding DataContext.SelectedTrackLaps, ElementName=Root}"
                                              AutoGenerateColumns="False"
                                              CanUserAddRows="False"
                                              IsReadOnly="True"
                                              Height="240"
                                              SelectedItem="{Binding DataContext.SelectedLap, ElementName=Root, Mode=TwoWay}">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="Lap" Binding="{Binding LapNumber}" Width="55" />
                                            <DataGridTextColumn Header="Time" Binding="{Binding LapTimeSeconds, Converter={StaticResource TimeSpanSecondsFormatter}}" Width="90" />
                                            <DataGridTextColumn Header="S1" Binding="{Binding Sector1Seconds, Converter={StaticResource TimeSpanSecondsFormatter}}" Width="80" />
                                            <DataGridTextColumn Header="S2" Binding="{Binding Sector2Seconds, Converter={StaticResource TimeSpanSecondsFormatter}}" Width="80" />
                                            <DataGridTextColumn Header="S3" Binding="{Binding Sector3Seconds, Converter={StaticResource TimeSpanSecondsFormatter}}" Width="80" />
                                            <DataGridCheckBoxColumn Header="Valid" Binding="{Binding IsValid}" Width="60" />
                                            <DataGridTextColumn Header="Recorded UTC" Binding="{Binding TimestampUtc, StringFormat={}{0:u}}" Width="160" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </StackPanel>
                            </styles:SHSection>
                        </StackPanel>
                    </DataTemplate>

                    <DataTemplate DataType="{x:Type local:StatsPlusSettingsTab}">
                        <StackPanel>
                            <styles:SHSection Title="General">
                                <StackPanel Margin="12">
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="190" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Text="Enable plugin" />
                                        <CheckBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" HorizontalAlignment="Left" IsChecked="{Binding DataContext.Settings.EnablePlugin, ElementName=Root, Mode=TwoWay}" />
                                        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Text="Publish game / track / car" />
                                        <CheckBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" HorizontalAlignment="Left" IsChecked="{Binding DataContext.Settings.PublishTrackInfo, ElementName=Root, Mode=TwoWay}" />
                                        <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,12,0" VerticalAlignment="Center" Text="Custom label" />
                                        <TextBox Grid.Row="2" Grid.Column="1" Width="220" HorizontalAlignment="Left" Text="{Binding DataContext.Settings.CustomLabel, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                    </Grid>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Width="120" Margin="0,0,10,0" Click="SaveButton_Click" Content="Save settings" />
                                        <Button Width="140" Click="ResetButton_Click" Content="Reset defaults" />
                                    </StackPanel>
                                </StackPanel>
                            </styles:SHSection>

                            <styles:SHSection Title="Enabled Games">
                                <StackPanel Margin="12">
                                    <TextBlock Margin="0,0,0,10" TextWrapping="Wrap">
                                        StatsPlus only records laps for the enabled games below. Unsupported or disabled games are ignored.
                                    </TextBlock>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="220" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Text="Assetto Corsa" />
                                        <CheckBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" HorizontalAlignment="Left" IsChecked="{Binding DataContext.Settings.RecordAssettoCorsa, ElementName=Root, Mode=TwoWay}" />
                                        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Text="Assetto Corsa Competizione" />
                                        <CheckBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" HorizontalAlignment="Left" IsChecked="{Binding DataContext.Settings.RecordAssettoCorsaCompetizione, ElementName=Root, Mode=TwoWay}" />
                                        <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Text="Assetto Corsa EVO" />
                                        <CheckBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,8" HorizontalAlignment="Left" IsChecked="{Binding DataContext.Settings.RecordAssettoCorsaEvo, ElementName=Root, Mode=TwoWay}" />
                                        <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Text="Automobilista 2" />
                                        <CheckBox Grid.Row="3" Grid.Column="1" Margin="0,0,0,8" HorizontalAlignment="Left" IsChecked="{Binding DataContext.Settings.RecordAutomobilista2, ElementName=Root, Mode=TwoWay}" />
                                        <TextBlock Grid.Row="4" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Text="iRacing" />
                                        <CheckBox Grid.Row="4" Grid.Column="1" Margin="0,0,0,8" HorizontalAlignment="Left" IsChecked="{Binding DataContext.Settings.RecordIRacing, ElementName=Root, Mode=TwoWay}" />
                                        <TextBlock Grid.Row="5" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Text="Le Mans Ultimate" />
                                        <CheckBox Grid.Row="5" Grid.Column="1" Margin="0,0,0,8" HorizontalAlignment="Left" IsChecked="{Binding DataContext.Settings.RecordLeMansUltimate, ElementName=Root, Mode=TwoWay}" />
                                        <TextBlock Grid.Row="6" Grid.Column="0" Margin="0,0,12,8" VerticalAlignment="Center" Text="rFactor 2" />
                                        <CheckBox Grid.Row="6" Grid.Column="1" Margin="0,0,0,8" HorizontalAlignment="Left" IsChecked="{Binding DataContext.Settings.RecordRFactor2, ElementName=Root, Mode=TwoWay}" />
                                        <TextBlock Grid.Row="7" Grid.Column="0" Margin="0,0,12,0" VerticalAlignment="Center" Text="RaceRoom / RRRE" />
                                        <CheckBox Grid.Row="7" Grid.Column="1" Margin="0,0,0,0" HorizontalAlignment="Left" IsChecked="{Binding DataContext.Settings.RecordR3E, ElementName=Root, Mode=TwoWay}" />
                                    </Grid>
                                </StackPanel>
                            </styles:SHSection>

                            <styles:SHSection Title="Debug Logging">
                                <StackPanel Margin="12">
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="190" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0"
                                                   Margin="0,0,12,0"
                                                   VerticalAlignment="Center"
                                                   Text="Debug logging" />
                                        <CheckBox Grid.Column="1"
                                                  HorizontalAlignment="Left"
                                                  Content="Write diagnostic debug log"
                                                  IsChecked="{Binding DataContext.IsDebugLoggingEnabled, ElementName=Root, Mode=TwoWay}" />
                                    </Grid>

                                    <TextBlock Margin="0,0,0,8" TextWrapping="Wrap">
                                        Diagnostic logs are written to separate files per game. Per-game logging options are available when debug logging is enabled.
                                    </TextBlock>

                                    <ItemsControl IsEnabled="{Binding DataContext.IsDebugLoggingEnabled, ElementName=Root}"
                                                  ItemsSource="{Binding DataContext.GameDebugLoggingOptions, ElementName=Root}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <CheckBox Margin="0,0,0,6"
                                                          Content="{Binding DisplayName}"
                                                          IsChecked="{Binding IsEnabled, Mode=TwoWay}" />
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </styles:SHSection>

                            <styles:SHSection Title="About">
                                <StackPanel Margin="12">
                                    <TextBlock FontWeight="Bold" Text="StatsPlus" />
                                    <TextBlock Margin="0,6,0,0" TextWrapping="Wrap">
                                        Records lap and sector times for the active game using a local LiteDB lap history store.
                                    </TextBlock>
                                    <TextBlock Margin="0,6,0,0" TextWrapping="Wrap">
                                        Published properties: StatsPlus.Version, StatsPlus.Enabled, StatsPlus.Label, StatsPlus.GameName,
                                        StatsPlus.TrackName, StatsPlus.CarModel, StatsPlus.SpeedKmh, StatsPlus.IsGameRunning,
                                        StatsPlus.LastLapTime, StatsPlus.SessionBestLapTime, StatsPlus.AllTimeBestLapTime,
                                        StatsPlus.SessionLapCount, StatsPlus.LastSector1Time, StatsPlus.LastSector2Time, StatsPlus.LastSector3Time
                                    </TextBlock>
                                    <TextBlock Margin="0,6,0,0" TextWrapping="Wrap">
                                        Stored personal bests are also exposed as StatsPlus.PersonalBest.{Game}.{Car}.{TrackVariation},
                                        using sanitized game, car, and TrackNameWithConfig segments for each recorded combination.
                                    </TextBlock>
                                </StackPanel>
                            </styles:SHSection>
                        </StackPanel>
                    </DataTemplate>
                </styles:SHTabControl.Resources>
            </styles:SHTabControl>
        </ScrollViewer>
    </Grid>
</UserControl>
